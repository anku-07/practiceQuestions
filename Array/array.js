/*
Create an array with fruits (e.g., apple, banana, orange).
Access the second element of the array.
Add an element to the end of the array and remove the first element.
*/
//Qn-1

let fruits = ['apple','banana','orange'];//Define a array

console.log(fruits[1]);

//Qn-2

console.log(fruits.push('mango'));

console.log(fruits.shift());