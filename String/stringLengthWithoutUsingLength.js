//Find and display the length of the string "Programming" without using the length property.
let str = "Programming";
let length = 0;
for(let char of str){
    length++;
}
console.log("Length of string : ",length);